import AdminCompanyManager from "@/components/admin/AdminCompanyManager";

/** Rota /admin/companies — gestão de empresas e dossiê de auditoria. */
export default function AdminCompaniesPage() {
  return <AdminCompanyManager />;
}
